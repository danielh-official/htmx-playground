{
  "name": "htmx-playground",
  "version": "1.0.0",
  "description": "Playing around with HTMX and a Flask backend server.",
  "main": "index.js",
  "scripts": {
    "format": "black . && prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "3.6.2"
  }
}
